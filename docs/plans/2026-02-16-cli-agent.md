# CLI Агент - План реализации

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Цель:** Создать интерактивный CLI агент на TypeScript с Ink для диалога с LLM через OpenRouter API с сохранением контекста.

**Архитектура:** Модульная структура с разделением на компоненты (Ink UI), API клиент (OpenRouter), управление историей сообщений. React-компонент управляет состоянием и взаимодействием, отправляет всю историю при каждом запросе для сохранения контекста.

**Tech Stack:** TypeScript, Ink, dotenv, OpenRouter API

---

## Task 1: Инициализация проекта

**Files:**
- Create: `day1/package.json`
- Create: `day1/tsconfig.json`
- Create: `day1/.gitignore`
- Create: `day1/.env.example`

**Step 1: Создать package.json**

```bash
cd day1
npm init -y
```

**Step 2: Установить зависимости**

```bash
npm install ink react dotenv
npm install -D @types/node @types/react tsx typescript
```

**Step 3: Создать tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "jsx": "react",
    "jsxFactory": "React.createElement",
    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true,
    "outDir": "./dist",
    "rootDir": "./src",
    "resolveJsonModule": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}
```

**Step 4: Создать .gitignore**

```
node_modules/
dist/
.env
*.log
```

**Step 5: Создать .env.example**

```
OPENROUTER_API_KEY=your_api_key_here
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet
```

**Step 6: Обновить package.json scripts**

Добавить в package.json:
```json
{
  "scripts": {
    "start": "tsx src/index.tsx",
    "build": "tsc"
  }
}
```

**Step 7: Создать структуру директорий**

```bash
mkdir -p src/components src/api src/chat src/types
```

**Step 8: Commit**

```bash
git add day1/
git commit -m "chore: инициализация проекта day1 с TypeScript и Ink

Настройка:
- package.json с зависимостями (ink, react, dotenv, typescript)
- tsconfig.json для TypeScript конфигурации
- .gitignore и .env.example
- Структура директорий src/

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

## Task 2: Типы и интерфейсы

**Files:**
- Create: `day1/src/types/index.ts`

**Step 1: Создать типы сообщений**

```typescript
export interface Message {
  role: 'user' | 'assistant';
  content: string;
}

export interface OpenRouterRequest {
  model: string;
  messages: Message[];
}

export interface OpenRouterResponse {
  choices: Array<{
    message: {
      role: string;
      content: string;
    };
  }>;
}
```

**Step 2: Commit**

```bash
git add day1/src/types/
git commit -m "feat: добавлены TypeScript типы для сообщений и API

Определены интерфейсы:
- Message для структуры сообщений
- OpenRouterRequest для запросов к API
- OpenRouterResponse для ответов от API

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

## Task 3: Управление историей сообщений

**Files:**
- Create: `day1/src/chat/conversation.ts`

**Step 1: Создать класс Conversation**

```typescript
import { Message } from '../types/index.js';

export class Conversation {
  private messages: Message[] = [];

  addUserMessage(content: string): void {
    this.messages.push({ role: 'user', content });
  }

  addAssistantMessage(content: string): void {
    this.messages.push({ role: 'assistant', content });
  }

  getHistory(): Message[] {
    return [...this.messages];
  }

  clear(): void {
    this.messages = [];
  }
}
```

**Step 2: Commit**

```bash
git add day1/src/chat/
git commit -m "feat: реализован класс Conversation для управления историей

Методы:
- addUserMessage() - добавление сообщения пользователя
- addAssistantMessage() - добавление ответа ассистента
- getHistory() - получение полной истории
- clear() - очистка истории

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

## Task 4: OpenRouter API клиент

**Files:**
- Create: `day1/src/api/openrouter.ts`

**Step 1: Создать функцию sendMessage**

```typescript
import { Message, OpenRouterRequest, OpenRouterResponse } from '../types/index.js';

export async function sendMessage(messages: Message[]): Promise<string> {
  const apiKey = process.env.OPENROUTER_API_KEY;
  const model = process.env.OPENROUTER_MODEL;

  if (!apiKey) {
    throw new Error('OPENROUTER_API_KEY не найден в переменных окружения');
  }

  if (!model) {
    throw new Error('OPENROUTER_MODEL не найден в переменных окружения');
  }

  const requestBody: OpenRouterRequest = {
    model,
    messages,
  };

  try {
    const response = await fetch('https://openrouter.ai/api/v1/chat/completions', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${apiKey}`,
      },
      body: JSON.stringify(requestBody),
    });

    if (!response.ok) {
      const errorText = await response.text();
      throw new Error(`OpenRouter API error (${response.status}): ${errorText}`);
    }

    const data: OpenRouterResponse = await response.json();

    if (!data.choices || !data.choices[0] || !data.choices[0].message) {
      throw new Error('Некорректный формат ответа от OpenRouter API');
    }

    return data.choices[0].message.content;
  } catch (error) {
    if (error instanceof Error) {
      throw new Error(`Ошибка при обращении к OpenRouter: ${error.message}`);
    }
    throw error;
  }
}
```

**Step 2: Commit**

```bash
git add day1/src/api/
git commit -m "feat: реализован OpenRouter API клиент

Функция sendMessage:
- Отправка истории сообщений в OpenRouter API
- Валидация переменных окружения (API_KEY, MODEL)
- Обработка ошибок сети и API
- Валидация формата ответа

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

## Task 5: Ink компонент Chat

**Files:**
- Create: `day1/src/components/Chat.tsx`

**Step 1: Создать компонент Chat**

```typescript
import React, { useState } from 'react';
import { Box, Text, useInput } from 'ink';
import { Conversation } from '../chat/conversation.js';
import { sendMessage } from '../api/openrouter.js';
import { Message } from '../types/index.js';

export const Chat: React.FC = () => {
  const [conversation] = useState(() => new Conversation());
  const [messages, setMessages] = useState<Message[]>([]);
  const [input, setInput] = useState('');
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  useInput(async (inputChar, key) => {
    if (isLoading) return;

    if (key.return) {
      if (input.trim()) {
        const userMessage = input.trim();
        setInput('');
        setError(null);

        conversation.addUserMessage(userMessage);
        setMessages(conversation.getHistory());
        setIsLoading(true);

        try {
          const response = await sendMessage(conversation.getHistory());
          conversation.addAssistantMessage(response);
          setMessages(conversation.getHistory());
        } catch (err) {
          const errorMessage = err instanceof Error ? err.message : 'Неизвестная ошибка';
          setError(errorMessage);
        } finally {
          setIsLoading(false);
        }
      }
    } else if (key.backspace || key.delete) {
      setInput((prev) => prev.slice(0, -1));
    } else if (!key.ctrl && !key.meta && inputChar) {
      setInput((prev) => prev + inputChar);
    }
  });

  return (
    <Box flexDirection="column" padding={1}>
      <Box flexDirection="column" marginBottom={1}>
        <Text bold color="cyan">
          CLI Агент (Ctrl+C для выхода)
        </Text>
        <Text dimColor>
          Модель: {process.env.OPENROUTER_MODEL || 'не указана'}
        </Text>
      </Box>

      <Box flexDirection="column" marginBottom={1}>
        {messages.map((msg, idx) => (
          <Box key={idx} marginBottom={1}>
            <Text bold color={msg.role === 'user' ? 'green' : 'blue'}>
              {msg.role === 'user' ? 'User' : 'Assistant'}:{' '}
            </Text>
            <Text>{msg.content}</Text>
          </Box>
        ))}
        {isLoading && (
          <Box>
            <Text bold color="blue">
              Assistant:{' '}
            </Text>
            <Text dimColor>[загрузка...]</Text>
          </Box>
        )}
      </Box>

      {error && (
        <Box marginBottom={1}>
          <Text color="red">Ошибка: {error}</Text>
        </Box>
      )}

      <Box>
        <Text color="yellow">&gt; </Text>
        <Text>{input}</Text>
        <Text backgroundColor="white"> </Text>
      </Box>
    </Box>
  );
};
```

**Step 2: Commit**

```bash
git add day1/src/components/
git commit -m "feat: реализован Ink компонент Chat для UI

Функциональность:
- useInput для обработки клавиатурного ввода
- Отображение истории сообщений с цветовой кодировкой
- Индикатор загрузки при ожидании ответа
- Отображение ошибок
- Интерактивное поле ввода с курсором

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

## Task 6: Точка входа приложения

**Files:**
- Create: `day1/src/index.tsx`

**Step 1: Создать точку входа**

```typescript
#!/usr/bin/env node
import React from 'react';
import { render } from 'ink';
import dotenv from 'dotenv';
import { Chat } from './components/Chat.js';

// Загрузка переменных окружения
dotenv.config();

// Проверка наличия необходимых переменных
if (!process.env.OPENROUTER_API_KEY) {
  console.error('Ошибка: OPENROUTER_API_KEY не найден в .env файле');
  console.error('Создайте .env файл на основе .env.example и укажите ваш API ключ');
  process.exit(1);
}

if (!process.env.OPENROUTER_MODEL) {
  console.error('Ошибка: OPENROUTER_MODEL не найден в .env файле');
  console.error('Создайте .env файл на основе .env.example и укажите модель');
  process.exit(1);
}

// Запуск приложения
render(<Chat />);
```

**Step 2: Commit**

```bash
git add day1/src/index.tsx
git commit -m "feat: добавлена точка входа приложения

Функциональность:
- Загрузка переменных окружения через dotenv
- Валидация наличия OPENROUTER_API_KEY и OPENROUTER_MODEL
- Запуск Ink приложения с компонентом Chat

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

## Task 7: README документация

**Files:**
- Create: `day1/README.md`

**Step 1: Создать README**

```markdown
# CLI Агент - День 1

Простой интерактивный CLI агент для взаимодействия с LLM через OpenRouter API.

## Возможности

- Интерактивная REPL сессия
- Сохранение контекста диалога
- Поддержка различных моделей через OpenRouter
- Цветной вывод в терминале

## Установка

1. Установите зависимости:
```bash
npm install
```

2. Создайте `.env` файл на основе `.env.example`:
```bash
cp .env.example .env
```

3. Укажите ваш OpenRouter API ключ в `.env`:
```
OPENROUTER_API_KEY=your_api_key_here
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet
```

## Использование

Запустите агент:
```bash
npm start
```

Введите ваше сообщение и нажмите Enter. Для выхода используйте Ctrl+C.

## Архитектура

```
src/
├── index.tsx              # Точка входа
├── components/
│   └── Chat.tsx           # Ink UI компонент
├── api/
│   └── openrouter.ts      # OpenRouter API клиент
├── chat/
│   └── conversation.ts    # Управление историей
└── types/
    └── index.ts           # TypeScript типы
```

## Технологии

- TypeScript
- Ink (React для CLI)
- OpenRouter API
- dotenv
```

**Step 2: Commit**

```bash
git add day1/README.md
git commit -m "docs: добавлена документация в README

Описаны:
- Возможности агента
- Инструкции по установке и настройке
- Использование
- Архитектура проекта
- Технологический стек

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

## Task 8: Финальное тестирование

**Step 1: Создать .env файл**

```bash
cd day1
cp .env.example .env
# Отредактируйте .env и добавьте ваш API ключ
```

**Step 2: Запустить приложение**

```bash
npm start
```

Ожидаемый результат:
- Приложение запускается без ошибок
- Отображается интерфейс с заголовком и полем ввода

**Step 3: Протестировать отправку сообщения**

Введите: "Привет!"
Нажмите: Enter

Ожидаемый результат:
- Сообщение добавляется в историю
- Отображается индикатор загрузки "[загрузка...]"
- Получен ответ от модели
- Ответ добавлен в историю

**Step 4: Протестировать контекст**

Введите: "Какое было мое предыдущее сообщение?"
Нажмите: Enter

Ожидаемый результат:
- Модель упоминает ваше сообщение "Привет!"
- Контекст сохраняется

**Step 5: Протестировать обработку ошибок**

Вариант A - неверный API ключ:
- Измените OPENROUTER_API_KEY в .env на неверный
- Запустите приложение
- Отправьте сообщение

Ожидаемый результат:
- Отображается ошибка с кодом 401

Вариант B - отсутствие .env:
- Переименуйте .env в .env.backup
- Запустите приложение

Ожидаемый результат:
- Приложение завершается с сообщением об отсутствии API ключа

**Step 6: Восстановить .env и финальный тест**

```bash
mv .env.backup .env  # если делали тест B
# Убедитесь что API ключ корректный
npm start
```

Проведите финальный диалог из 3-5 сообщений, убедитесь что всё работает стабильно.

**Step 7: Финальный commit**

```bash
git add .
git commit -m "test: финальное тестирование CLI агента

Проверено:
- Запуск приложения
- Отправка сообщений и получение ответов
- Сохранение контекста между сообщениями
- Обработка ошибок (неверный API ключ, отсутствие .env)
- Стабильность при длинных диалогах

CLI агент готов к использованию.

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

---

## Итоговая проверка

- ✓ Проект инициализирован с правильной структурой
- ✓ TypeScript типы определены
- ✓ Класс Conversation управляет историей
- ✓ OpenRouter API клиент работает
- ✓ Ink компонент отображает UI
- ✓ Точка входа валидирует конфигурацию
- ✓ README документация написана
- ✓ Тестирование пройдено
- ✓ Все изменения закоммичены

**Готово к использованию!**
